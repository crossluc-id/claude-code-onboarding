{
  "_comment": "Copy to ~/.claude/.mcp.json. Set EXA_API_KEY and GITHUB_TOKEN in ~/.zshrc. Keep under 10 MCPs to preserve context window.",
  "mcpServers": {
    "exa": {
      "command": "npx",
      "args": ["-y", "exa-mcp-server"],
      "description": "Exa AI neural search — deep web research, code context, academic search (needs EXA_API_KEY in shell)"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "description": "GitHub API — repos, PRs, issues, code search (needs GITHUB_TOKEN in shell)"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "REPLACE_WITH_YOUR_HOME_DIRECTORY"],
      "description": "Enhanced filesystem operations — read, write, search files. IMPORTANT: Replace REPLACE_WITH_YOUR_HOME_DIRECTORY with your actual home path (e.g., /Users/yourname on macOS, /home/yourname on Linux)"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Multi-step reasoning — helps Claude think through complex problems step by step"
    }
  }
}
